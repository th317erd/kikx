# User Settings UI Polish
# Deferred from: Phase 6 (Auth + User Settings)
# Priority: Medium
# Complexity: Low-Medium

summary: >
  Polish the kikx-settings component with full tab functionality,
  form validation, and integration with all backend APIs.

current_state: >
  kikx-settings component exists with tabs (Profile, Account, API Keys,
  Permissions, Billing). Basic routing works (F4 complete). Profile and
  password forms are wired. API key management UI is stubbed.
  The backend APIs are fully functional.

requirements:
  - Profile tab: display name, email edit with duplicate check, avatar preview
  - Account tab: password change with confirmation field, magic link setup
  - API Keys tab: create with name + scope checkboxes, list with last_used_at, revoke with confirmation
  - Permissions tab: list personal rules, show auto-approved actions, revoke grants
  - Billing tab: read-only usage display from token_charges table
  - Form validation on all inputs (client-side + server error display)
  - Toast/notification on save success/failure

files:
  - public/js/components/kikx-settings/kikx-settings.js
  - public/js/components/kikx-settings/kikx-settings.html
  - public/js/api.js                     # API.user namespace (already exists)

tests:
  - Each tab renders correct form fields
  - Profile save sends PUT to /api/users/me/profile
  - API key create shows plaintext once, then hides
  - Permission revoke calls DELETE /api/permissions/:id
  - Form validation prevents empty required fields
