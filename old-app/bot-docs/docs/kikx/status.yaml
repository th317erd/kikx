# Kikx Implementation Status - Compressed YAML
# Branch: feature/mythix-ui-migration
# s=status, p=pending, ts=tests

meta:
  branch: feature/mythix-ui-migration
  total_tests: 1672
  test_failures: 0
  test_runner: "find spec -name '*-spec.mjs' | xargs node --test --test-force-exit"

phases:
  0: {t: Frame Migration, s: COMPLETE}
  1: {t: Multi-party Sessions, s: SERVER COMPLETE, p: [sidebar, "@mention", WS broadcast]}
  2: {t: Permissions System, s: CORE COMPLETE, p: [prompt UX, meta-permissions, structured args]}
  3: {t: Agent Roles & Coordination, s: CORE COMPLETE, p: [streaming, multi-coord, "@mention"]}
  4: {t: Commands + Plugin Hardening, s: CORE COMPLETE, p: [npm plugins, fs.watch]}
  5: {t: HML Forms + Infinite Scroll, s: CORE COMPLETE}
  6: {t: Auth Enhancement, s: CORE COMPLETE, p: [settings UI, scope enforcement]}
  7: {t: Server-Authoritative Hardening, s: CORE COMPLETE, p: [self-approval, nonce, chain UX]}
  8: {t: File Uploads Avatars Rich Content, s: CORE COMPLETE, p: [screenshots, picker, renderers]}

post_phase8_fixes:
  - {t: Interaction detection, d: attribute-format tags not matched}
  - {t: UI bubble layout, d: avatar+username inside bubble, user default avatar, reflection chevron}
  - {t: Websearch permissions, d: streaming path bypassed approval}
  - {t: Result frame cleanup, d: hide successful result JSON}
  - {t: User avatar initial, d: username-based not "You"}
  - {t: Draft persistence, d: sessionStorage with debounce}
  - {t: Create Agent modal, d: removed auto-chain to Create Session}

deferred:
  phase1:
    - Participant sidebar
    - "@mention autocomplete"
    - WS broadcast to all participants
  phase2:
    - BEFORE_TOOL hook
    - "Permission prompt UX (<hml-prompt>)"
    - Meta-permissions
    - Structured command args
  phase3:
    - Inter-agent streaming
    - Multi-coordinator discussion
    - "@mention routing"
  phase4:
    - npm package plugin support
    - fs.watch auto-start in server
  phase6:
    - User settings UI component
    - API key scope enforcement in middleware
  phase7:
    - Self-approval prevention
    - Cross-session nonce
    - Chained command permissions UX
  phase8:
    - Screenshots (plugin)
    - Avatar picker UI
    - Rich content renderers (plugin)
