'use strict';

// =============================================================================
// Plugins — Public API
// =============================================================================
// ClaudeAgent has been extracted to plugins/claude-agent/ (project root).
// Only AgentInterface remains as the core plugin base class.
// =============================================================================

export { AgentInterface } from './agent-interface.mjs';
