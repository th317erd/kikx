{
  "name": "kikx",
  "version": "0.1.0",
  "description": "Modular agent runner backed by AeorDB",
  "type": "module",
  "main": "src/index.mjs",
  "scripts": {
    "start": "node src/server/index.mjs",
    "test": "timeout 120 node --test --test-timeout=30000 'spec/**/*-spec.mjs'"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "GPL-3.0-or-later"
}

